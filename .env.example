# ── PostgreSQL ────────────────────────────────────────────────────────────────
# Used by docker-compose.yml (dev) to configure the postgres container.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=file-processing-platform-db
POSTGRES_PORT=5432